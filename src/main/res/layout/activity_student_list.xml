<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Checkboxes for All Present and All Absent -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingBottom="8dp">

        <CheckBox
            android:id="@+id/allPresentCheckBox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="All Present"
            android:textSize="18sp"
            android:paddingEnd="16dp" />

        <CheckBox
            android:id="@+id/allAbsentCheckBox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="All Absent"
            android:textSize="18sp" />
    </LinearLayout>

    <!-- ListView for displaying students with individual checkboxes -->
    <ListView
        android:id="@+id/studentListView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp" />

    <!-- Submit Attendance Button -->
    <Button
        android:id="@+id/submitAttendanceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Submit Attendance"
        android:textSize="18sp"
        android:padding="12dp" />

</LinearLayout>

